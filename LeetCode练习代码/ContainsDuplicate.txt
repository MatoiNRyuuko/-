给定一个整数数组，判断是否存在重复元素。

如果任何值在数组中出现至少两次，函数返回 true。如果数组中每个元素都不相同，则返回 false。

示例 1:

输入: [1,2,3,1]
输出: true

思路：看到题目马上想到暴力的枚举，但是那样太慢了。看了别人答案学到了Arrays.sort()方法，java自带的数组
排序方法，将数组排序后只需要一遍遍历就能判断出来了。

简单的题目能学到东西也要记录下来。