给定一个字符串，找到它的第一个不重复的字符，并返回它的索引。如果不存在，则返回 -1。

案例:

s = "leetcode"
返回 0.

s = "loveleetcode",
返回 2.
 

注意事项：您可以假定该字符串只包含小写字母。

思路：暴力枚举的做法很容易，但是想复杂度低地实现就想了很久。在这里的做法是参考了网上的解法，定义一个大小26
数组记录每个数字出现的次数，到时候找到数字出现次数为1的字母的位置就行了（hashmap）。