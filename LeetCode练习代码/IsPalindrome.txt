给定一个字符串，验证它是否是回文串，只考虑字母和数字字符，可以忽略字母的大小写。

说明：本题中，我们将空字符串定义为有效的回文串。

示例 1:

输入: "A man, a plan, a canal: Panama"
输出: true

思路：目前想到两种做法，第一种是将字符串转为char数组，然后逐个判断如果是数字或字母就往list里面放。然后两个
指针从list的头和尾同时开始比较，如果不相等就false。这种方法比较慢。
第二种方法是，把字符串做成char数组，直接双指针开始比较，如果其中一根指针碰到不是数字或者字母的数，就再走一位。
在比较之前先将字符转成小写的。这种比较快。