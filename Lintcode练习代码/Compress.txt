213. 字符串压缩

设计一种方法，通过给重复字符计数来进行基本的字符串压缩。

例如，字符串 aabcccccaaa 可压缩为 a2b1c5a3 。而如果压缩后的字符数不小于原始的字符数，则返回原始的字符串。

可以假设字符串仅包括a-z的字母。

样例

str=aabcccccaaa 返回 a2b1c5a3
str=aabbcc 返回 aabbcc
str=aaaa 返回 a4

输入测试数据 (每行一个参数)

思路：把字符串做成char数组逐个比较，但是返回提交的运行时间比较长，字符串的处理要多练习。