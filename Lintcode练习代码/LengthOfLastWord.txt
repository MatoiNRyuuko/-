给定一个字符串， 包含大小写字母、空格' '，请返回其最后一个单词的长度。

如果不存在最后一个单词，请返回 0 。

 注意事项

一个单词的界定是，由字母组成，但不包含任何的空格。

样例
给定 s = "Hello World"，返回 5。

思路：不太懂这道题考的什么。。是因为java把字符串转成数组这道题太容易了吧。
字符串转成数组，从尾到头循环判断，有空格就返回i就行了，如果第一个是空格
i另算。