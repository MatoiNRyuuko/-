给定一个整数数组，找到一个具有最大和的子数组，返回其最大和。

样例
给出数组[?2,2,?3,4,?1,2,1,?5,3]，符合要求的子数组为[4,?1,2,1]，其最大和为6

挑战 
要求时间复杂度为O(n)

思路：第一次想完成挑战的做法是找到数组最大的数往左边累加和往右边累加，取大的那边的值。
但是发现这样不行，数据通过了77%。这种做法时间复杂度虽然是O（n）但是只适用特别小的数组。
大的数组中的最大子数组和不一定包括这个数组的最大数。

后来发现是贪心策略，只需遍历一遍数组，将大于0且最大子数组和记录下来，小于0的子数组和直接放弃。
（当然还是暴力枚举最简单但是太暴力了啊！）