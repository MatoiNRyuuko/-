给定一个数组和一个值，在原地删除与值相同的数字，返回新数组的长度。

元素的顺序可以改变，并且对新的数组不会有影响。

样例
给出一个数组 [0,4,4,0,0,2,4,4]，和值 4

返回 4 并且4个元素的新数组为[0,0,0,2]

思路：这道题之前居然困扰我了几个小时。。简直不可思议。今天看了下标签，两根
指针，几分钟就做出来了，看来刷题也是有效果的。

一个用来替换数字的指针，一个用来循环的指针。当循环指针遇到4时，替换数字指针
不动。当遇到的不是4时，循环指针的值赋给替换指针，替换指针++。