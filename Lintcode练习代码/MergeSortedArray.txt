合并两个排序的整数数组A和B变成一个新的数组。

你可以假设A具有足够的空间（A数组的大小大于或等于m+n）去添加B中的元素。

样例
给出 A = [1, 2, 3, empty, empty], B = [4, 5]

合并之后 A 将变成 [1,2,3,4,5]

思路：归并排序的简化版，因为原数组已经排好序了。两个指针分别指向两个数组头，
比较指针数的大小放进list，操作指针移动，最后再将完整的list数放回数组就行了。