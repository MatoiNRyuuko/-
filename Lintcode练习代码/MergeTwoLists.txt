将两个排序链表合并为一个新的排序链表.

样例
给出 1->3->8->11->15->null，2->null， 返回 1->2->3->8->11->15->null。

思路：链表节点引用的控制，想法很基础，但是涉及到这么多条件的判定，提交上
lintcode就很容易超时。代码中带1的是自己写的版本，应该是对的，不过超时了。
2是网上别人成功提交的版本（运行时间29708ms不超时？）。
所以这道题考的不是问题思路而是代码思路，把心思花在代码的效率上。