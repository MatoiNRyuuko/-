给定一个整型数组，找出主元素，它在数组中的出现次数严格大于数组元素个数的二分之一。

样例
给出数组[1,1,1,1,2,2,2]，返回 1



思路：由于题目规定了是主元素严格大于二分之一的，不会等于也不会小于。那么所有不和主元素
相等数的和主元素一对一抵消之后，肯定还会剩余主元素。利用这个思想对数组进行遍历，并且
为主元素的个数定义一个变量，如果遍历的数和主元素相等，变量+1。如果变量=0了，就让现在
遍历的这个元素暂时成为主元素，因为可能会出现数组不是主元素的数暂时多于主元素。

但是无论如何，最后返回的一定是主元素。
